# Lista de IPs bloqueadas - Generado automáticamente
# Formato: deny <IP>;
# Ejemplo:
# deny 192.168.1.100;  # Bloqueada por ataque SQL injection
# deny 10.0.0.50;      # Bloqueada por brute force

# Agregar IPs a bloquear debajo de esta línea
